<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward all requests into the public/ directory (front controller lives there).
    RewriteRule ^(\.well-known/.*)$ - [L]
    RewriteCond %{REQUEST_URI} !^/?public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
